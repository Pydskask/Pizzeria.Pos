<Application x:Class="Pizzeria.Pos.Wpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>

        <!-- =========================
             PALETA
        ========================== -->
        <SolidColorBrush x:Key="Brush.WindowBg" Color="#0B1220"/>
        <SolidColorBrush x:Key="Brush.PanelBg" Color="#0F172A"/>
        <SolidColorBrush x:Key="Brush.PanelAltBg" Color="#111827"/>
        <SolidColorBrush x:Key="Brush.CardBg" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="Brush.CardBorder" Color="#CBD5E1"/>

        <SolidColorBrush x:Key="Brush.TextPrimaryDark" Color="#0F172A"/>
        <SolidColorBrush x:Key="Brush.TextMutedDark" Color="#475569"/>
        <SolidColorBrush x:Key="Brush.TextPrimaryLight" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="Brush.TextMutedLight" Color="#94A3B8"/>

        <SolidColorBrush x:Key="Brush.Primary" Color="#2563EB"/>
        <SolidColorBrush x:Key="Brush.PrimaryHover" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="Brush.Success" Color="#10B981"/>
        <SolidColorBrush x:Key="Brush.SuccessHover" Color="#059669"/>
        <SolidColorBrush x:Key="Brush.Warning" Color="#F59E0B"/>
        <SolidColorBrush x:Key="Brush.WarningHover" Color="#D97706"/>
        <SolidColorBrush x:Key="Brush.Danger" Color="#DC2626"/>
        <SolidColorBrush x:Key="Brush.DangerHover" Color="#B91C1C"/>
        <SolidColorBrush x:Key="Brush.Secondary" Color="#475569"/>
        <SolidColorBrush x:Key="Brush.SecondaryHover" Color="#334155"/>
        <SolidColorBrush x:Key="Brush.Info" Color="#0EA5E9"/>
        <SolidColorBrush x:Key="Brush.InfoHover" Color="#0284C7"/>

        <SolidColorBrush x:Key="Brush.ListAltRow" Color="#F1F5F9"/>
        <SolidColorBrush x:Key="Brush.ListSelection" Color="#DBEAFE"/>

        <!-- =========================
             TYPOGRAFIA
        ========================== -->
        <FontFamily x:Key="TouchFont">Segoe UI</FontFamily>

        <!-- =========================
             BAZOWY BUTTON
        ========================== -->
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource TouchFont}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource Brush.Secondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="2"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- =========================
             TOUCH BUTTON
        ========================== -->
        <Style x:Key="TouchButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Height" Value="70"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Background" Value="{StaticResource Brush.Primary}"/>
        </Style>

        <Style x:Key="SuccessTouchButton" TargetType="Button" BasedOn="{StaticResource TouchButton}">
            <Setter Property="Background" Value="{StaticResource Brush.Success}"/>
        </Style>

        <Style x:Key="DangerTouchButton" TargetType="Button" BasedOn="{StaticResource TouchButton}">
            <Setter Property="Background" Value="{StaticResource Brush.Danger}"/>
        </Style>

        <Style x:Key="WarningTouchButton" TargetType="Button" BasedOn="{StaticResource TouchButton}">
            <Setter Property="Background" Value="{StaticResource Brush.Warning}"/>
        </Style>

        <Style x:Key="SecondaryTouchButton" TargetType="Button" BasedOn="{StaticResource TouchButton}">
            <Setter Property="Background" Value="{StaticResource Brush.Secondary}"/>
        </Style>

        <Style x:Key="KeypadButtonBaseStyle"
               TargetType="Button"
               BasedOn="{StaticResource TouchButton}">
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource Brush.Primary}"/>
        </Style>

        <!-- =========================
             TEXTBOX
        ========================== -->
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="{StaticResource TouchFont}"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="Height" Value="52"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimaryDark}"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource Brush.CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
                                <Setter TargetName="Root" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.65"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- =========================
             COMBOBOX
        ========================== -->
        <Style TargetType="ComboBox">
            <Setter Property="FontFamily" Value="{StaticResource TouchFont}"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Padding" Value="12,6"/>
        </Style>

        <!-- =========================
             DATEPICKER
        ========================== -->
        <Style TargetType="DatePicker">
            <Setter Property="FontFamily" Value="{StaticResource TouchFont}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- =========================
             CHECKBOX
        ========================== -->
        <Style TargetType="CheckBox">
            <Setter Property="FontFamily" Value="{StaticResource TouchFont}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- =========================
             LISTBOX
        ========================== -->
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="{StaticResource TouchFont}"/>
            <Setter Property="FontSize" Value="18"/>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Root"
                                Background="White"
                                BorderBrush="{StaticResource Brush.CardBorder}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource Brush.ListSelection}"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
                                <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimaryDark}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource Brush.ListAltRow}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- =========================
             DATAGRID
        ========================== -->
        <Style TargetType="DataGrid">
            <Setter Property="FontFamily" Value="{StaticResource TouchFont}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="RowHeight" Value="52"/>
            <Setter Property="ColumnHeaderHeight" Value="50"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource Brush.ListAltRow}"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Brush.CardBorder}"/>
            <Setter Property="VerticalGridLinesBrush" Value="{StaticResource Brush.CardBorder}"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource Brush.PanelBg}"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimaryLight}"/>
            <Setter Property="FontFamily" Value="{StaticResource TouchFont}"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="{StaticResource Brush.PanelBg}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- =========================
             TEXTBLOCK
        ========================== -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource TouchFont}"/>
        </Style>

        <!-- =========================
             SCROLLVIEWER
        ========================== -->
        <Style TargetType="ScrollViewer">
            <Setter Property="Focusable" Value="False"/>
        </Style>

        <SolidColorBrush x:Key="UiButtonBrush" Color="#355C8A"/>
        <SolidColorBrush x:Key="UiButtonHoverBrush" Color="#416A99"/>
        <SolidColorBrush x:Key="UiButtonPressedBrush" Color="#2C4D73"/>
        <SolidColorBrush x:Key="UiButtonBorderBrush" Color="#274766"/>

        <Style x:Key="SquareButton" TargetType="Button">
            <Setter Property="Height" Value="70"/>
            <Setter Property="Width" Value="160"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource UiButtonBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource UiButtonBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource UiButtonHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource UiButtonPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SmallMenuButton" TargetType="Button" BasedOn="{StaticResource SquareButton}">
            <Setter Property="Height" Value="72"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Margin" Value="4"/>
        </Style>

        <Style x:Key="SectionTileButton" TargetType="Button" BasedOn="{StaticResource SquareButton}">
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="92"/>
            <Setter Property="FontSize" Value="18"/>
        </Style>

        <Style x:Key="SalesMainButton" TargetType="Button" BasedOn="{StaticResource SquareButton}">
            <Setter Property="Width" Value="220"/>
            <Setter Property="Height" Value="120"/>
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="Margin" Value="10"/>
        </Style>

    </Application.Resources>
</Application>